# 番茄时钟 Flutter 应用实现计划

## 概述

### 我们要解决什么问题？
开发一个功能完整的番茄时钟（Pomodoro Timer）Flutter 应用，帮助用户通过番茄工作法提高工作效率和专注力。番茄工作法是一种时间管理方法，通过将工作分解为25分钟的专注时段（番茄钟），中间穿插短暂休息，以提高生产力。

### 成功标准
- ✅ 用户可以启动、暂停和重置番茄钟计时器
- ✅ 支持自定义工作时长、短休息时长和长休息时长
- ✅ 完成4个番茄钟后自动触发长休息
- ✅ 提供直观的视觉反馈和进度显示
- ✅ 计时结束时提供声音和视觉通知
- ✅ 记录和展示番茄钟历史统计
- ✅ 界面简洁美观，符合 Material Design 规范
- ✅ 支持 iOS 和 Android 双平台

### 目标用户
- 需要提高工作专注力的职场人士
- 学生群体进行学习时间管理
- 自由职业者进行项目时间管理
- 任何希望使用番茄工作法的用户

## 技术方案

### 核心架构
**架构模式**: BLoC (Business Logic Component) 模式
- **展示层**: Flutter Widgets（UI组件）
- **业务逻辑层**: BLoC 处理状态管理和业务逻辑
- **数据层**: 本地存储（SharedPreferences/Hive）用于设置和历史记录

**主要技术选型**:
```yaml
Flutter SDK: >=3.0.0
主要依赖包:
  - flutter_bloc: ^8.1.3        # 状态管理
  - equatable: ^2.0.5           # 值对象比较
  - hive: ^2.2.3                # 本地数据库
  - hive_flutter: ^1.1.0        # Hive Flutter 集成
  - audioplayers: ^5.2.0        # 音频播放
  - flutter_local_notifications: ^16.1.0  # 本地通知
  - circular_countdown_timer: ^0.2.3     # 圆形倒计时UI
  - fl_chart: ^0.64.0           # 统计图表
  - intl: ^0.18.1               # 国际化支持
```

### 核心数据模型

#### 1. TimerState（计时器状态）
```dart
enum TimerStatus { initial, running, paused, completed }

class TimerState {
  final int duration;          // 当前时长（秒）
  final TimerStatus status;    // 状态
  final SessionType sessionType; // 会话类型
  final int completedPomodoros; // 已完成的番茄数
}
```

#### 2. SessionType（会话类型）
```dart
enum SessionType {
  work,       // 工作时段
  shortBreak, // 短休息
  longBreak   // 长休息
}
```

#### 3. PomodoroSettings（设置）
```dart
class PomodoroSettings {
  final int workDuration;        // 工作时长（分钟）- 默认25
  final int shortBreakDuration;  // 短休息时长（分钟）- 默认5
  final int longBreakDuration;   // 长休息时长（分钟）- 默认15
  final int longBreakInterval;   // 长休息间隔（番茄数）- 默认4
  final bool soundEnabled;       // 是否启用声音
  final bool notificationEnabled; // 是否启用通知
  final String theme;            // 主题颜色
}
```

#### 4. PomodoroSession（历史记录）
```dart
class PomodoroSession {
  final String id;
  final DateTime startTime;
  final DateTime endTime;
  final SessionType type;
  final bool completed;         // 是否完整完成
  final int duration;           // 实际时长（秒）
}
```

### 主要功能模块

#### 1. 计时器核心 (Timer Core)
- **Ticker**: 提供秒级时间流
- **TimerBloc**: 管理计时器状态和逻辑
- **计时器事件**:
  - TimerStarted: 开始计时
  - TimerPaused: 暂停计时
  - TimerResumed: 恢复计时
  - TimerReset: 重置计时器
  - TimerTicked: 时间流逝（每秒触发）
  - TimerCompleted: 计时完成

#### 2. 设置管理 (Settings)
- **SettingsBloc**: 管理用户设置
- **持久化存储**: 使用 Hive 保存设置
- **设置项**:
  - 自定义各时段时长
  - 长休息触发条件
  - 声音开关
  - 通知开关
  - 主题选择

#### 3. 历史统计 (Statistics)
- **StatisticsBloc**: 管理统计数据
- **数据展示**:
  - 今日完成番茄数
  - 本周完成番茄数
  - 总计完成番茄数
  - 专注时长统计
  - 图表可视化（每日趋势、每周对比）

#### 4. 通知系统 (Notifications)
- **本地通知**: 使用 flutter_local_notifications
- **通知场景**:
  - 工作时段结束提醒
  - 休息时段结束提醒
  - 背景运行时的持续通知

### 技术决策与权衡

| 决策项 | 选择方案 | 替代方案 | 理由 |
|--------|----------|----------|------|
| 状态管理 | BLoC | Provider / Riverpod | BLoC 提供清晰的事件流和状态流，适合复杂的计时器逻辑 |
| 本地存储 | Hive | SQLite / SharedPreferences | Hive 性能优秀，API 简洁，适合结构化数据存储 |
| 音频播放 | audioplayers | just_audio | audioplayers 文档完善，社区支持好 |
| UI 框架 | Material Design | Cupertino / 自定义 | Material Design 跨平台一致性好，组件丰富 |

## 实现计划

### 第一阶段：基础框架搭建（2-3天）

**目标**: 建立项目基础结构和核心依赖

#### 任务清单
1. **项目初始化** [小]
   - 创建 Flutter 项目：`flutter create pomodoro_timer`
   - 配置 pubspec.yaml，添加所有必需依赖
   - 设置项目目录结构
   ```
   lib/
   ├── main.dart
   ├── bloc/              # BLoC 状态管理
   ├── models/            # 数据模型
   ├── repositories/      # 数据仓库
   ├── screens/           # 页面
   ├── widgets/           # 可复用组件
   ├── services/          # 服务（通知、音频等）
   └── utils/             # 工具类
   ```

2. **数据模型定义** [小]
   - 创建 `TimerState` 模型
   - 创建 `SessionType` 枚举
   - 创建 `PomodoroSettings` 模型
   - 创建 `PomodoroSession` 模型
   - 使用 Equatable 实现值对象比较

3. **Hive 数据库初始化** [中]
   - 设置 Hive 适配器
   - 创建 TypeAdapter for PomodoroSettings
   - 创建 TypeAdapter for PomodoroSession
   - 初始化数据库盒子（boxes）

4. **Ticker 服务** [小]
   - 实现 Ticker 类，提供周期性时间流
   - 单元测试 Ticker 功能

**依赖关系**: 任务按顺序执行
**交付物**: 
- 可运行的 Flutter 项目骨架
- 数据模型定义完成
- 本地存储初始化完成

---

### 第二阶段：核心计时器功能（3-4天）

**目标**: 实现完整的计时器核心逻辑

#### 任务清单
1. **TimerBloc 实现** [大]
   - 定义计时器事件（Events）
   - 定义计时器状态（States）
   - 实现状态转换逻辑
   - 处理计时器开始、暂停、恢复、重置
   - 实现自动切换工作/休息时段
   - 实现长休息触发逻辑（每4个番茄钟）

2. **计时器 UI 开发** [大]
   - 创建主屏幕（HomeScreen）
   - 实现圆形倒计时显示
   - 添加开始/暂停/重置按钮
   - 显示当前会话类型（工作/休息）
   - 显示已完成番茄数量
   - 进度环动画效果

3. **音频服务** [中]
   - 实现 AudioService 类
   - 添加提示音资源文件
   - 实现计时完成时播放提示音
   - 支持音量控制

4. **TimerBloc 单元测试** [中]
   - 测试状态转换逻辑
   - 测试自动切换功能
   - 测试边界情况

**依赖关系**: 
- TimerBloc 必须先于 UI 开发完成
- 音频服务可并行开发

**交付物**:
- 功能完整的计时器
- 可以开始、暂停、重置
- 自动切换工作/休息时段
- 计时完成时播放提示音

---

### 第三阶段：设置与个性化（2-3天）

**目标**: 实现用户自定义设置功能

#### 任务清单
1. **SettingsBloc 实现** [中]
   - 定义设置事件和状态
   - 实现设置加载和保存逻辑
   - 与 Hive 集成，持久化设置
   - 提供默认设置值

2. **设置页面 UI** [大]
   - 创建设置页面（SettingsScreen）
   - 工作时长设置（滑块或输入框）
   - 短休息时长设置
   - 长休息时长设置
   - 长休息间隔设置
   - 声音开关
   - 通知开关
   - 主题颜色选择器

3. **主题系统** [中]
   - 定义多个主题配色
   - 实现主题切换功能
   - 保存用户主题偏好

4. **设置集成** [中]
   - 将设置应用到 TimerBloc
   - 确保设置更改立即生效
   - 添加设置重置功能

**依赖关系**: SettingsBloc 必须先完成，然后开发 UI

**交付物**:
- 完整的设置功能
- 用户可自定义所有时长参数
- 设置持久化保存
- 主题切换功能

---

### 第四阶段：通知系统（2天）

**目标**: 实现本地通知功能

#### 任务清单
1. **通知服务初始化** [中]
   - 配置 Android 通知渠道
   - 配置 iOS 通知权限
   - 创建 NotificationService 类

2. **通知功能实现** [中]
   - 实现计时完成通知
   - 实现前台服务通知（Android）
   - 添加通知点击处理
   - 支持通知声音和振动

3. **权限处理** [小]
   - 请求通知权限（iOS）
   - 处理权限拒绝情况
   - 引导用户开启权限

**依赖关系**: 依赖第二阶段的 TimerBloc

**交付物**:
- 完整的本地通知系统
- 计时结束时发送通知
- 支持前台服务（防止被系统杀死）

---

### 第五阶段：历史统计功能（3-4天）

**目标**: 实现番茄钟历史记录和统计分析

#### 任务清单
1. **StatisticsBloc 实现** [大]
   - 定义统计事件和状态
   - 实现历史记录保存逻辑
   - 实现统计数据计算
   - 按日期聚合数据

2. **HistoryRepository** [中]
   - 实现会话记录的 CRUD 操作
   - 与 Hive 集成
   - 实现数据查询和过滤

3. **统计页面 UI** [大]
   - 创建统计页面（StatisticsScreen）
   - 今日统计卡片（完成数、专注时长）
   - 本周统计卡片
   - 历史总计
   - 使用 fl_chart 绘制图表
     - 每日完成趋势折线图
     - 每周完成柱状图
   - 历史记录列表

4. **数据导出功能** [小]（可选）
   - 导出为 CSV 格式
   - 分享功能

**依赖关系**: 
- StatisticsBloc 和 Repository 必须先完成
- UI 开发依赖于数据层

**交付物**:
- 完整的统计分析功能
- 可视化图表
- 历史记录查看

---

### 第六阶段：优化与完善（2-3天）

**目标**: 提升用户体验，修复问题，准备发布

#### 任务清单
1. **UI/UX 优化** [中]
   - 添加页面切换动画
   - 优化按钮触感反馈
   - 改善加载状态显示
   - 添加空状态提示
   - 优化深色模式支持

2. **错误处理** [中]
   - 添加全局错误捕获
   - 实现错误提示 UI
   - 处理音频播放失败
   - 处理存储失败

3. **性能优化** [中]
   - 优化 BLoC 事件处理
   - 减少不必要的重建
   - 优化数据库查询
   - 内存泄漏检查

4. **多语言支持** [中]（可选）
   - 配置 intl 包
   - 提取所有文本字符串
   - 提供中文和英文翻译
   - 实现语言切换

5. **测试** [大]
   - Widget 测试核心 UI 组件
   - Integration 测试主要流程
   - 手动测试 iOS 和 Android
   - 修复发现的 Bug

6. **文档与发布准备** [小]
   - 编写 README.md
   - 添加应用图标
   - 配置启动画面
   - 准备应用商店截图
   - 编写版本说明

**依赖关系**: 依赖所有前期功能完成

**交付物**:
- 经过优化和测试的完整应用
- 完整的应用文档
- 准备发布的 APK/IPA

---

## 详细技术规范

### 应用架构图
```
┌─────────────────────────────────────────────┐
│              Presentation Layer              │
│  (Screens, Widgets, UI Components)          │
└───────────────┬─────────────────────────────┘
                │ BLoC Pattern
┌───────────────▼─────────────────────────────┐
│           Business Logic Layer               │
│  (TimerBloc, SettingsBloc, StatisticsBloc)  │
└───────────────┬─────────────────────────────┘
                │ Repository Pattern
┌───────────────▼─────────────────────────────┐
│              Data Layer                      │
│  (Hive Database, SharedPreferences)         │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│            Services Layer                    │
│  (AudioService, NotificationService)        │
└─────────────────────────────────────────────┘
```

### 核心 API 设计

#### TimerBloc API
```dart
// Events
sealed class TimerEvent {
  const TimerEvent();
}

class TimerStarted extends TimerEvent {
  final int duration;
  final SessionType sessionType;
}

class TimerPaused extends TimerEvent {}
class TimerResumed extends TimerEvent {}
class TimerReset extends TimerEvent {}
class TimerTicked extends TimerEvent {
  final int duration;
}

// States
sealed class TimerState extends Equatable {
  final int duration;
  final SessionType sessionType;
  final int completedPomodoros;
  
  const TimerState({
    required this.duration,
    required this.sessionType,
    required this.completedPomodoros,
  });
}

class TimerInitial extends TimerState {}
class TimerRunInProgress extends TimerState {}
class TimerRunPause extends TimerState {}
class TimerRunComplete extends TimerState {}
```

#### SettingsRepository API
```dart
abstract class SettingsRepository {
  Future<PomodoroSettings> getSettings();
  Future<void> saveSettings(PomodoroSettings settings);
  Future<void> resetSettings();
  Stream<PomodoroSettings> watchSettings();
}
```

#### HistoryRepository API
```dart
abstract class HistoryRepository {
  Future<void> saveSession(PomodoroSession session);
  Future<List<PomodoroSession>> getSessions({
    DateTime? startDate,
    DateTime? endDate,
  });
  Future<void> deleteSession(String id);
  Future<void> clearAllSessions();
  Future<Statistics> getStatistics(DateRange range);
}
```

### 页面结构

```
主应用
├── HomePage (底部导航栏)
│   ├── TimerTab (计时器页面) 🍅
│   ├── StatisticsTab (统计页面) 📊
│   └── SettingsTab (设置页面) ⚙️
└── 各页面内容
```

### 数据库结构

#### Settings Box
```
Key: 'user_settings'
Value: PomodoroSettings {
  workDuration: int,
  shortBreakDuration: int,
  longBreakDuration: int,
  longBreakInterval: int,
  soundEnabled: bool,
  notificationEnabled: bool,
  theme: String
}
```

#### History Box
```
Key: session_id (UUID)
Value: PomodoroSession {
  id: String,
  startTime: DateTime,
  endTime: DateTime,
  type: SessionType,
  completed: bool,
  duration: int
}
```

### UI 设计规范

#### 颜色方案
```dart
// 工作时段主题
primaryColor: Color(0xFFE74C3C)  // 番茄红
accentColor: Color(0xFFFF6B6B)

// 短休息主题
primaryColor: Color(0xFF3498DB)  // 天蓝色
accentColor: Color(0xFF5DADE2)

// 长休息主题
primaryColor: Color(0xFF2ECC71)  // 绿色
accentColor: Color(0xFF58D68D)
```

#### 字体规范
- 标题: 32px, Bold
- 计时器显示: 72px, Bold, Monospace
- 正文: 16px, Regular
- 小字: 14px, Regular

## 关键考量因素

### 假设前提
1. 用户设备支持 Flutter 3.0+
2. 用户授予应用通知权限
3. 设备有足够的存储空间（< 50MB）
4. 用户理解番茄工作法的基本概念
5. 不需要云同步功能（初版）
6. 不需要用户账号系统（初版）

### 约束条件
- **时间约束**: 整体开发周期约 2-3 周
- **技术约束**: 
  - 仅支持移动端（iOS/Android）
  - 不支持 Web 和桌面端
  - 需要 iOS 10+ 和 Android 5.0+
- **资源约束**:
  - 单人或小团队开发
  - 开源项目，无商业预算
- **功能约束**:
  - 初版不包含社交功能
  - 初版不包含云备份

### 风险与应对

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|----------|
| 后台计时器被系统杀死 | 高 | 中 | 使用前台服务（Android）和后台模式（iOS），添加重连逻辑 |
| 通知权限被拒绝 | 中 | 中 | 提供明确的权限说明，支持应用内提醒作为备选 |
| Hive 数据迁移问题 | 中 | 低 | 版本化数据模型，提供迁移脚本 |
| 不同设备音频播放失败 | 低 | 低 | 提供多种音频格式，添加静默失败处理 |
| 电池消耗过高 | 中 | 低 | 优化 Ticker 频率，使用系统定时器 |
| 性能问题（低端设备） | 低 | 低 | 测试并优化关键路径，减少动画复杂度 |

### 测试策略

#### 单元测试
- TimerBloc 状态转换逻辑
- SettingsBloc 设置管理逻辑
- StatisticsBloc 统计计算逻辑
- Repository 数据操作
- Utility 函数

#### Widget 测试
- 计时器 UI 组件
- 设置页面表单
- 统计图表渲染

#### 集成测试
- 完整的番茄钟流程（开始-完成-自动切换）
- 设置更改并保存
- 历史记录保存和查询

#### 手动测试检查清单
- ✅ 前台运行完整番茄钟流程
- ✅ 后台运行保持计时
- ✅ 应用被杀死后恢复状态
- ✅ 通知正常显示和点击
- ✅ 音频正常播放
- ✅ 设置正确保存和加载
- ✅ 统计数据准确
- ✅ 旋转屏幕状态保持
- ✅ 深色模式显示正常
- ✅ 低端设备性能可接受

## 不包含的功能（后续版本）

### V1.0 不包含
- ❌ 用户账号系统
- ❌ 云端数据同步
- ❌ 社交功能（分享、排行榜）
- ❌ 任务管理集成
- ❌ 自定义提示音
- ❌ Apple Watch / Wear OS 支持
- ❌ 桌面小部件
- ❌ 番茄钟标签/分类
- ❌ 团队协作功能
- ❌ 高级统计（生产力分析、专注度评分）
- ❌ 集成第三方应用（日历、待办事项）
- ❌ 网页版

### 可能的未来增强
- 🔮 多设备同步（V2.0）
- 🔮 任务列表集成（V2.0）
- 🔮 更丰富的统计分析（V2.0）
- 🔮 自定义主题和提示音（V2.0）
- 🔮 桌面小部件（V2.5）
- 🔮 智能手表支持（V3.0）
- 🔮 AI 驱动的生产力建议（V3.0）

## 发布计划

### Alpha 版本（内部测试）
- 完成核心计时器功能
- 基础 UI
- 本地测试通过

### Beta 版本（外部测试）
- 所有核心功能完成
- 通知系统工作
- 统计功能可用
- 邀请 10-20 位用户测试

### V1.0 正式版
- 所有计划功能完成
- Bug 修复
- 性能优化
- 完整文档
- 应用商店发布

## 估算时间表

| 阶段 | 工作日 | 日历周 |
|------|--------|--------|
| 第一阶段：基础框架 | 2-3 天 | 0.5 周 |
| 第二阶段：核心计时器 | 3-4 天 | 0.5-1 周 |
| 第三阶段：设置功能 | 2-3 天 | 0.5 周 |
| 第四阶段：通知系统 | 2 天 | 0.5 周 |
| 第五阶段：统计功能 | 3-4 天 | 0.5-1 周 |
| 第六阶段：优化完善 | 2-3 天 | 0.5 周 |
| **总计** | **14-19 天** | **3-4 周** |

*注：基于单人全职开发估算，如果是兼职或团队协作，时间表需相应调整*

## 资源需求

### 开发工具
- Flutter SDK 3.0+
- Android Studio / VS Code
- Xcode (Mac, iOS 开发)
- Git 版本控制

### 设计资源
- 应用图标设计
- 提示音文件（可使用免费资源）
- UI 设计参考（Material Design）

### 测试设备
- Android 真机（至少 1 台）
- iOS 真机（至少 1 台，如需 iOS 版本）
- 不同屏幕尺寸的测试

### 外部服务
- GitHub / GitLab（代码托管）
- Google Play Console（Android 发布）
- Apple Developer Program（iOS 发布，需 99 USD/年）

## 开始开发

### 快速启动指南
```bash
# 1. 创建项目
flutter create pomodoro_timer
cd pomodoro_timer

# 2. 添加依赖到 pubspec.yaml
flutter pub add flutter_bloc equatable hive hive_flutter
flutter pub add audioplayers flutter_local_notifications
flutter pub add circular_countdown_timer fl_chart intl

# 3. 生成 Hive 适配器（在添加模型后）
flutter pub add --dev hive_generator build_runner
flutter packages pub run build_runner build

# 4. 运行项目
flutter run

# 5. 测试
flutter test
```

### 第一个里程碑检查点
完成第一阶段后，你应该拥有：
- ✅ 可运行的 Flutter 项目
- ✅ 完整的项目目录结构
- ✅ 所有数据模型定义
- ✅ Hive 数据库初始化完成
- ✅ Ticker 服务实现并测试通过

### 持续改进
开发过程中应该：
- 📝 每天记录进度和遇到的问题
- 🧪 编写测试，保持代码质量
- 📚 阅读用户反馈，记录改进想法
- 🔄 定期重构，保持代码整洁
- 🎨 关注 UI/UX，持续优化体验

---

## 附录

### 参考资源

#### Flutter 官方文档
- [Flutter 中文网](https://flutter.cn/)
- [BLoC 官方文档](https://bloclibrary.dev/)
- [Hive 文档](https://docs.hivedb.dev/)

#### 设计灵感
- [番茄工作法官网](https://francescocirillo.com/pages/pomodoro-technique)
- [Material Design](https://material.io/)
- [Dribbble - Timer App Designs](https://dribbble.com/tags/timer_app)

#### 类似应用参考
- Focus To-Do（功能全面）
- Forest（游戏化）
- Be Focused（简洁设计）

### 常见问题

**Q: 为什么选择 BLoC 而不是 Provider？**
A: BLoC 提供了更清晰的事件-状态流，适合处理复杂的计时器逻辑和状态转换。Provider 更轻量，但在复杂场景下可能导致代码混乱。

**Q: 是否需要后端服务器？**
A: V1.0 不需要。所有数据存储在本地，无需后端。如果未来需要云同步功能，再考虑集成 Firebase 或自建后端。

**Q: 如何确保后台计时准确性？**
A: 使用系统时间戳而非累计计数，记录开始时间，每次计算剩余时间 = 总时长 - (当前时间 - 开始时间)。

**Q: 如何处理应用被系统杀死的情况？**
A: 使用前台服务（Android）保持运行；在暂停时保存状态；恢复时检查时间戳，计算应有状态。

**Q: 开发 iOS 版本是必须的吗？**
A: 不是。可以先专注于 Android 版本，验证核心功能和市场反馈后，再投入 iOS 开发。

---

*此实现计划是一个活文档，应该随着开发进展和新发现不断更新和完善。*

**最后更新**: 2025-11-09
**文档版本**: 1.0
**目标读者**: 开发团队、产品经理、利益相关者
